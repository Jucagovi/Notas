import React from "react";
import useDatos from "../../hooks/useDatos";

const DesplegableCiclos = () => {
  const { ciclos } = useDatos();

  return <div>DesplegableCiclos</div>;
};

export default DesplegableCiclos;
